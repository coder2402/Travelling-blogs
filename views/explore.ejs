<!DOCTYPE html>
<html lang="en">

<head>
    <%-include('_head')%>
    <title>Safar | Explore</title>
    <link rel="stylesheet" href="/css/explore.css">
    <script src="https://kit.fontawesome.com/e9cb9b0869.js" crossorigin="anonymous"></script>
</head>

<body>
    <%-include('header')%>
        <div class="main">
            <% if (!reviews || reviews.length === 0) { %>
                <div class="review" style="justify-content: center; align-items: center; flex-direction: column;">
                    <p class="user">No travel experiences yet!</p>
                    <p class="data" style="margin-top: 10px; color: white;">Be the first to share your journey.</p>
                    <a class="delete-btn" href="/new-review" style="width: auto; text-align: center; display: block; padding: 5px 20px;" aria-label="Add a new review">Add Review</a>
                </div>
            <% } else { %>
            <% for(let i of reviews){ %>
                <div class="review">
                    <div class="left">
                        <img class="img" src="<%=i.image%>" alt="Image for <%=i.location%> posted by <%=i.username%>">
                        <p class="user">
                            <%=i.username%>
                        </p>
                    </div>
                    <div class="right">
                        <p class="data"><span>Location: </span>
                            <%=i.location%>
                        </p>
                        <p class="data"><span>Experience:</span>
                            <%=i.experience%>
                        </p>
                        <p class="data"><span>Places to Visit:</span>
                            <%=i.places%>
                        </p>
                        <p class="data"><span>Rating:</span>
                            <%=i.rating%>
                        </p>
                        <p class="data"><span>Expenditure:</span>
                            <%=i.expenditure%>
                        </p>
                        <a class="delete-btn" href="/delete-review/?id=<%= i._id %>" onclick="return confirm('Are you sure you want to delete this review?');" aria-label="Delete review for <%=i.location%> by <%=i.username%>">Delete <i class="fas fa-trash" aria-hidden="true"></i></a>
                    </div>
                </div>
                <% } %>
            <% } %>
        </div>
        <%-include('footer')%>
</body>

</html>
